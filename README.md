# Lab 12: Todo List Mockup (10 pts)

ป้อนข้อมูลนักศึกษา
รหัส นศ.: 670610429

ชื่อ-สกุล : ศิลา เสนาพงษ์

## เช็กก่อนส่ง

> **อย่าลืม** เปลี่ยนชื่อ-นามสกุล (fullName) และรหัสนักศึกษา (studentId) ใน `Footer` ให้เป็นของตัวเอง

> **อย่าลืม** เปลี่ยนชื่อ (userName) และ type ใน `Sidebar` ให้เป็นของตัวเอง

```bash
pnpm run build   # ต้องผ่าน (ไม่มี error) — Vercel ใช้คำสั่งนี้ deploy
pnpm run lint    # ควรผ่าน
```
